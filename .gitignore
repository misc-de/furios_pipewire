# Build-Ausgaben
poc/spa-droid/build/
poc/spa-droid/generated/
*.o
*.so

# Upstream sources: cloned separately, see README (not versioned here)
src/pulseaudio-modules-droid-modern/

# Generated configuration (derived from the FuriOS template)
pipewire-hal.conf

# Test recordings and build artefacts do not belong in the repo - a
# microphone recording from a test run is the owner's ambient audio.
*.wav
*.deb
__pycache__/
*.pyc
